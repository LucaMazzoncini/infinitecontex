# Supervised-agent benchmark

`generate_fixture.py` creates a disposable original asynchronous scheduler. `visible/` is grant-visible; `evaluator/` is excluded and contains hidden contract checks. Generated runs belong outside the tracked repository or under an ignored directory. Phase A stops at a mutation candidate and never applies it.
